<!DOCTYPE html>

<title>Edit Secretaire</title>

{% block body %}
    <h1>Edit Secretaire</h1>

    {{ include('secretaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_secretaire_index') }}">back to list</a>

    {{ include('secretaire/_delete_form.html.twig') }}
{% endblock %}
