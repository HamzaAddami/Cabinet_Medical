<!DOCTYPE html>

<title>Ordonance index</title>

{% block body %}
    <h1>Ordonance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateOrdo</th>
                <th>Medicament</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ordonance in ordonances %}
            <tr>
                <td>{{ ordonance.id }}</td>
                <td>{{ ordonance.dateOrdo ? ordonance.dateOrdo|date('Y-m-d') : '' }}</td>
                <td>{{ ordonance.medicament }}</td>
                <td>
                    <a href="{{ path('app_ordonance_show', {'id': ordonance.id}) }}">show</a>
                    <a href="{{ path('app_ordonance_edit', {'id': ordonance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ordonance_new') }}">Create new</a>
{% endblock %}
